
version: "2"

networks:
  db:
    internal: true
  exposed:
    internal: false

services:
  writer:
    build: .
    networks:
        - db
        - exposed
    ports:
      - 5240:5240
    environment:
      - NASANOV_SECRET
      - MODE=writer
      - PORT=5240
      - INFLUX_HOST=influx
  reader:
    build: .
    networks:
        - db
        - exposed
    expose:
      - 9090
    ports:
      - 5250:5250
    environment:
      - MODE=reader
      - PORT=5250
      - INFLUX_HOST=influx
      - LISTENER_HOST=reader
      - LISTENER_PORT=9090
  influx:
    image: influxdb
    volumes:
        - ./:/var/lib/influxdb
    networks:
      - db
    expose:
      - 8086